// src/IndicadoresExtras.js
import React from "react";

export default function IndicadoresExtras() {
  return (
    <div style={{ padding: "16px" }}>
      <h2>Indicadores</h2>
      <p>
        Tela pronta para receber os dados da aba <b>INDICADORES_EXTRAS</b> da planilha.
        Na próxima etapa, vamos ler o JSON do Supabase e renderizar as tabelas/quadros.
      </p>
    </div>
  );
}
